var price = 30;
// price = price + 10;
price +=10; // price = price + 10 both are same 
console.log(price)

var nemPrice = 50;
// nemPrice = nemPrice - 20;
 nemPrice -= 20; ///nemPrice = nemPrice -20 both are smae

console.log(nemPrice)